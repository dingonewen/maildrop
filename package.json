{
  "name": "maildrop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:setup": "wrangler d1 migrations apply maildrop-db --local",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "postal-mime": "^2.7.5"
  }
}
